/** The single spelling of "the run is parked on a person", shared by the project
 *  stage badge and the timeline gate badge so the two cannot drift apart. */
export const PENDING_REVIEW_LABEL = 'Pending Review';
/** The house review-tier token — already the review stage's color; no new token. */
export const PENDING_REVIEW_CSS_VAR = '--tier-review';
