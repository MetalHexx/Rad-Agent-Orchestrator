namespace: example
version: "1.0.0"
description: "Reference custom node-type package — a single zero-capability greeting node."
nodeTypes:
  - name: example:greet
    entrypoint: ./dist/greet.js
# templates: reserved for a future iteration (D20) — not loaded in 2.5.
