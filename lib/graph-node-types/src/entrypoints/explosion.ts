// A thin disk-artifact entrypoint — see `phase.ts` for the shape/rationale shared by all nine.
// `EXPLOSION_NODE_TYPE` is the `createExplosionNodeType()` result the barrel already exports.
export { EXPLOSION_NODE_TYPE as default } from '../rad-orc/explosion.js';
